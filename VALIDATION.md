# 규칙 검증 결과 (v1.2)

검증일: 2026-08-21 · 검증 방법: 규칙 6절 — "이 규칙을 이 규칙으로 만들어 본다."

## 1. 자기 적용 결과

Product를 "PBS WBS Board 규칙"으로 정해 루트 게시글 000을 올리고, 필요를
001~005로 쪼개 v1.0을 완료했다. 이후 두 번의 불편이 규칙 7절에 따라 각각
게시글로 올라와 개정되었다 — 개정 자체가 규칙대로 처리된 것이 곧 개정 절차의
검증이다.

- v1.1 (`board/006`): Work가 시간 축에서 보이지 않고 병렬 진행이 명시되지
  않았다 → `after`/`started`/`finished` 필드와 Gantt 뷰, "병렬이 기본" 규칙.
- v1.2 (`board/007`): 개정 006이 00:20에 끝났는데 루트 000의 finished가
  00:06에 머물러 최종 Work가 타임라인 중간에 있었고, Gantt에서 `after`
  종속성이 텍스트로만 보였다 → "취합 Work는 마지막에 끝난다" 규칙(부모
  재취합·finished 갱신), Gantt 종속성 화살표, 도구의 취합 순서 경고.
  v1.2 절차에 따라 007을 올릴 때 000을 TAKEN으로 되돌렸고, 007이 DONE된 뒤
  000을 재취합해 finished(00:33)가 전체의 마지막이 되었다.

- WBS: `board/` 게시글 트리 (000 아래 001~007)
- PBS: RULES.md, board/, tools/build_board_view.py, board.html, VALIDATION.md
- 병렬 기록: 002와 003이 같은 구간(00:03~00:04)에 병렬로 진행되어 Gantt에서
  겹친 막대로 보인다. 순서 제약은 `after` 화살표로만 표현된다.
- 취합 순서: 루트 000의 finished(00:33)가 모든 자식(마지막 007, 00:32)보다
  늦다. `build_board_view.py`가 위반 시 경고를 출력함을 이전 위반 상태
  (000이 00:06 DONE)로 재현해 확인했다.
- 종료 조건 충족: OPEN 게시글 0건, 루트 게시글 000 DONE → **규칙 v1.2 완료**

## 2. rule 스킬 여섯 문장 대조

| 기준 | 판정 | 근거 |
|---|---|---|
| 협업의 불편함을 없애는가 | 충족 | 일을 주고받는 단일 통로(Board)와 상태 전이를 고정해, 에이전트·사람 간 인수인계 방식이 갈리지 않는다. |
| 용어를 정의하는가 | 충족 | RULES.md 1절에서 Product/Work/산출물/Board/게시글/PBS/WBS를 표로 정의. |
| 다 숙지하지 않아도 따라할 수 있는가 | 충족 | RULES.md 2절 "바로 따라하기" 3단계만으로 참여 가능. |
| 스스로 검증되는가 | 충족 | 이 문서가 그 증거다. 규칙이 규칙 자신을 Product로 삼아 만들어졌고(board/000~007), 종료 조건과 취합 순서가 기계적으로 판정된다(build_board_view.py가 완료 여부와 위반 경고를 출력). |
| 적용 범위와 끝이 예상되는가 | 충족 | 범위는 "Product가 정해진 하나의 프로젝트", 끝은 "OPEN 0건 + 루트 DONE"으로 명시. |
| 이 규칙도 그렇도록 노력하는가 | 충족 | 7절에서 개정 자체를 게시글(Work)로 처리하고 새 버전도 같은 방식으로 재검증하도록 했다. v1.1·v1.2가 실제로 그렇게 만들어졌다. |

## 3. 요구사항 대조

- Product를 Work로 나눈다 → 2절 절차, 4절 분해 규칙. ✔
- Work는 항상 산출물을 낸다 → 4절 "산출물 없는 Work는 없다", 경로 없으면 DONE 불가. ✔
- 필요한 것을 Board에 올리고, 다른 에이전트가 받아 다시 쪼갠다 → 2절 집기/쪼개기 반복. ✔
- 산출물을 시간·구조로 합치면 PBS → 5절, board.html의 PBS 트리와 시간 순 목록. ✔
- Work들이 Gantt처럼 보인다 → 3절 started/finished 기록, board.html의 Gantt 차트. ✔
- 여러 Work가 병렬로 진행될 수 있다 → 4절 "병렬이 기본", `after`에 없는 순서는
  없는 것으로 간주. ✔
- **최종 Work(취합)는 시간상 항상 마지막이다** → 4절 "취합 Work는 마지막에
  끝난다", 위반 시 도구가 경고. 루트 000이 실제로 마지막(00:33)이다. ✔
- **Work 간 종속성이 Gantt에서 보인다** → 5절, board.html Gantt의 `after`
  화살표(선행의 끝 → 후행의 시작). ✔
- PBS/WBS가 미리 정해져 있을 수 있다 → 1절 마지막 문단(미리 게시글로 올려둠). ✔
- 계속 검토하며 발전 → 7절 개정 규칙(개정도 Work, 재검증 필수). ✔
- 사람도 지켜본다 → 5절 HTML 뷰, tools/build_board_view.py → board.html. ✔

## 4. 남은 과제

다음 개정 때 검토할 후보(불편이 실제로 확인되면 게시글로 올린다):

- 여러 저장소·여러 Product를 하나의 Board로 다루는 경우의 범위 확장.
- 예정 일정(계획 시작/끝)을 Gantt에 함께 표시하는 필드.

해결된 과제:

- 게시글 간 선후 관계 표기법 → v1.1의 `after` 필드.
- 선후 관계의 시각화 → v1.2의 Gantt 종속성 화살표.
- 최종 산출물이 타임라인 중간에 오는 문제 → v1.2의 취합 마감 규칙(재취합·경고).
