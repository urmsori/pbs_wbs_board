# 규칙 검증 결과 (v1.3)

검증일: 2026-08-21 · 검증 방법: 규칙 6절 — "이 규칙을 이 규칙으로 만들어 본다."

## 1. 자기 적용 결과

Product를 "PBS WBS Board 규칙"으로 정해 루트 게시글 000을 올리고, 필요를
001~005로 쪼개 v1.0을 완료했다. 이후 세 번의 불편이 규칙 7절에 따라 각각
게시글로 올라와 개정되었다 — 개정 자체가 규칙대로 처리된 것이 곧 개정 절차의
검증이다.

- v1.1 (`board/006`): Work가 시간 축에서 보이지 않고 병렬 진행이 명시되지
  않았다 → `after`/`started`/`finished` 필드와 Gantt 뷰, "병렬이 기본" 규칙.
- v1.2 (`board/007`): 최종 Work가 타임라인 중간에 있었고 종속성이 보이지
  않았다 → "취합 Work는 마지막에 끝난다" 규칙, Gantt 종속성 화살표, 취합
  순서 경고.
- v1.3 (`board/008`): 2절이 분해 관점("쪼개거나 끝내기")만 담아 여러 명이
  각자 반복해 도는 **사이클**로 읽히지 않았고, 산출물이 게시글당 경로 1개로
  Product와 1:1처럼 보였으며, 검증이 절차에 없었다 → 2절을 6단계 사이클
  (집기 → 필요 올리기 → 모이면 수행·검증 → 부족하면 다시 올리기 → 산출물
  PBS 기록 → 반복)로 다시 쓰고, 다중·부분 산출물과 "PBS — Product 구성"
  (중복 제거) 뷰, "수행만으로는 DONE이 아니다" 규칙을 추가했다.

### v1.3 개정이 곧 새 사이클의 검증이다

개정 008 자체를 새 6단계 사이클대로 진행했고, 각 단계가 실제로 발생했다:

| 사이클 단계 | 실제 기록 |
|---|---|
| 1. 집기 | 008을 TAKEN (00:36) |
| 2. 필요 올리기 | 필요 009(문안)·010(도구)을 008의 자식으로 게시 |
| 3. 모이면 수행·검증 | 009(00:39)·010(00:41) DONE 후 008의 수행·검증 진행 |
| 4. 부족하면 다시 올리기 | **검증 중 실제 발견**: 상태 문구가 새 "끝" 정의와 어긋남 → 011 게시·해결 (00:44) |
| 5. 산출물 기록 (PBS) | 008이 다중 산출물 "RULES.md, VALIDATION.md"로 DONE — 두 파일 모두 기존 PBS 항목의 갱신이라 Product 구성 목록은 변하지 않았다 |
| 6. 반복 | 남은 Work(000 재취합)를 집어 마무리 |

- 산출물 ≠ Product 1:1의 증거: RULES.md는 Work 001·006·007·009가 반복
  갱신했지만 PBS의 Product 구성에는 한 번만 나타난다. 008은 산출물이 2개다.
  011은 도구의 일부만 고쳤고 Product 구성 목록을 바꾸지 않았다.
- 검증 기록: 009·010·011 본문에 각자의 검증 방법·결과가 남아 있다(4절 규칙).
- 취합 순서: 루트 000의 finished가 전체의 마지막이고, 도구가 위반을 경고한다.
- 종료 조건 충족: OPEN·TAKEN 게시글 0건, 루트 게시글 000 DONE → **규칙 v1.3 완료**

## 2. rule 스킬 여섯 문장 대조

| 기준 | 판정 | 근거 |
|---|---|---|
| 협업의 불편함을 없애는가 | 충족 | 여러 명이 각자 도는 사이클과 단일 통로(Board)·상태 전이를 고정해, 에이전트·사람 간 인수인계 방식이 갈리지 않는다. |
| 용어를 정의하는가 | 충족 | RULES.md 1절에서 Product/Work/산출물/Board/게시글/PBS/WBS를 표로 정의. 산출물과 Product의 관계(1:1 아님)도 정의에 포함. |
| 다 숙지하지 않아도 따라할 수 있는가 | 충족 | RULES.md 2절 사이클 6단계만 돌면 참여 가능. |
| 스스로 검증되는가 | 충족 | 이 문서가 그 증거다. v1.3 사이클 개정 자체가 새 사이클대로 진행되었고(위 표), 종료 조건·취합 순서·산출물 유무가 기계적으로 판정된다(build_board_view.py). |
| 적용 범위와 끝이 예상되는가 | 충족 | 범위는 "Product가 정해진 하나의 프로젝트", 끝은 "OPEN·TAKEN 0건 + 루트 DONE"으로 명시. |
| 이 규칙도 그렇도록 노력하는가 | 충족 | 7절에서 개정 자체를 게시글(Work)로 처리하고 새 버전도 같은 방식으로 재검증하도록 했다. v1.1~v1.3이 실제로 그렇게 만들어졌다. |

## 3. 요구사항 대조

- Product를 Work로 나눈다 → 2절 사이클, 4절 분해 규칙. ✔
- Work는 항상 산출물을 낸다 → 4절, 경로 없으면 DONE 불가(도구 경고). ✔
- 필요한 것을 Board에 올리고, 다른 에이전트가 받아 다시 쪼갠다 → 2절 사이클 2·4단계. ✔
- **여러 명이 사이클을 계속 돌며 WBS/PBS를 만든다** → 2절 "여러 명이 이
  사이클을 각자 병렬로 돈다", 6단계 반복. ✔
- **Work를 잡고 → 필요를 올리고 → 모이면 수행·검증하고 → 검증 중 필요를 다시
  올리고 → 산출물을 PBS에 기록하고 → 반복** → 2절 1~6단계와 1:1 대응, 008의
  실제 진행 기록(위 표)으로 검증. ✔
- **산출물은 Product와 1:1이 아니다(일부분·여러 개·부분 갱신)** → 1절 정의,
  4절 규칙, deliverable 쉼표 구분, PBS Product 구성의 중복 제거. 011이 부분
  갱신 사례. ✔
- 수행뿐 아니라 **검증**이 사이클에 있다 → 2절 3·5단계, 4절 "수행만으로는
  DONE이 아니다". ✔
- 산출물을 시간·구조로 합치면 PBS → 5절, board.html의 Product 구성·분해
  트리·시간 순 목록. ✔
- Work들이 Gantt처럼 보인다, 종속성 화살표, 병렬 → v1.1·v1.2 기능 유지. ✔
- 최종 Work(취합)는 시간상 항상 마지막 → 4절, 루트 000이 실제로 마지막. ✔
- PBS/WBS가 미리 정해져 있을 수 있다 → 1절 마지막 문단. ✔
- 계속 검토하며 발전 → 7절, v1.1~v1.3의 실적. ✔
- 사람도 지켜본다 → 5절 HTML 뷰. ✔

## 4. 남은 과제

다음 개정 때 검토할 후보(불편이 실제로 확인되면 게시글로 올린다):

- 여러 저장소·여러 Product를 하나의 Board로 다루는 경우의 범위 확장.
- 예정 일정(계획 시작/끝)을 Gantt에 함께 표시하는 필드.
- 에이전트가 실제로 여러 명일 때의 push 충돌 빈도 관찰(3절 규칙의 실전 검증).

해결된 과제:

- 게시글 간 선후 관계 표기법 → v1.1의 `after` 필드.
- 선후 관계의 시각화 → v1.2의 Gantt 종속성 화살표.
- 최종 산출물이 타임라인 중간에 오는 문제 → v1.2의 취합 마감 규칙.
- 반복 사이클·검증 단계·다중/부분 산출물 → v1.3의 사이클과 PBS 기록.
