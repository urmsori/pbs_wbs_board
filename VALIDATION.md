# 규칙 검증 결과 (v1.8)

검증일: 2026-08-21 · 검증 방법: 규칙 6절 — "이 규칙을 이 규칙으로 만들어 본다"
+ 별도 프로젝트(인공위성 예시)에 적용하며 개정한다.

## 1. 자기 적용 결과

Product를 "PBS WBS Board 규칙"으로 정해 루트 게시글 000을 올리고 v1.0을
완료한 뒤, 모든 개정을 규칙 7절대로 게시글로 처리했다. 루트 000은 개정이
생길 때마다 TAKEN으로 되돌아갔다가 재취합되어 finished가 항상 마지막이다.

| 버전 | 게시글 | 불편(트리거) → 반영 |
|---|---|---|
| v1.1 | 006 | 시간축·병렬이 안 보임 → after/started/finished, Gantt, 병렬 기본 |
| v1.2 | 007 | 최종 Work가 타임라인 중간, 종속성 안 보임 → 취합 마감 규칙, Gantt 화살표, 취합 경고 |
| v1.3 | 008~011 | 사이클로 안 읽힘, 산출물 1:1 가정, 검증 부재 → 6단계 사이클, 다중·부분 산출물, PBS Product 구성 |
| v1.4 | 012 | 위성 예시를 올릴 보드가 없음 → 프로젝트마다 보드, id 접두어, 도구 인자화 |
| v1.5 | 013 | 모듈 단계가 흩어져 보임, 병렬 에이전트 충돌 예상(상대의 불편 선제 수정) → track 배지, 단계도 Work, 파일 소유·웨이브 취합 |
| v1.6 | 014 | **웨이브 1 에이전트 4명의 실보고** → 초 단위 시각, 집기 가능 표시, 기록의 정본, 잠금 시점 |
| v1.7 | 015 | **웨이브 2 에이전트 실보고** → 읽기 전용 --ready 조회, 시각 정합·버전 줄 검사 |
| v1.8 | 016 | **취합자(agent-ait) 실보고** → 취합 시 자식 산출물 간 모순 처리 |

## 2. 위성 예시에 의한 검증 (examples/satellite)

규칙을 규칙 아닌 Product에 적용한 첫 사례. 에이전트 6명(claude, agent-str/
eps/aocs/comm/ait)이 각자 사이클을 병렬로 돌아 게시글 16건을 전부 DONE으로
만들었고, FRR 판정 "K-SAT 1 FM 인도 가능"으로 끝났다(OPEN·TAKEN 0건 + 루트
S000 DONE·마지막 finished).

- **최종 형상만 EM/QM/FM**(S002→S003→S004), 모듈은 각자 단계: STR은 STM→FM,
  EPS는 BB→EM→FM, AOCS는 시뮬→EM→FM, COMM은 EM→QM→FM. 규칙은 단계 집합을
  강제하지 않는다(4절 "단계도 Work다") — 예시가 그 증거다.
- **병렬 실증**: 웨이브 1에서 4개 모듈 트랙이 동시 진행(00:53~00:55 겹침),
  웨이브 2에서 EM 형상 통합(00:59:24~01:00:09)이 FM 모듈 제작
  (00:59:27~01:00:19)과 겹쳐 진행 — Gantt의 겹친 막대와 after 화살표로 보인다.
- **규칙 개정 루프의 실증**: 에이전트들이 사이클을 돌며 보고한 불편이 그대로
  개정 게시글이 되었다(위 표의 v1.6~v1.8). 특히 "RULES.md 버전 줄이 이력과
  어긋난다"는 웨이브 2의 보고는 실제 결함이었고, 정정 후 같은 실수를 도구
  경고(v1.7)로 막았다.
- **다중·부분 산출물**: S000은 산출물 2개(README, FRR 문서), 모듈 단계들은
  각자 파일 — PBS Product 구성에서 경로별 갱신 Work와 마지막 갱신 시각으로
  취합된다.

## 3. rule 스킬 여섯 문장 대조

| 기준 | 판정 | 근거 |
|---|---|---|
| 협업의 불편함을 없애는가 | 충족 | 단일 통로(보드)·상태 전이·사이클 고정. 에이전트 6명이 규칙 텍스트만으로 충돌 없이 협업했다. |
| 용어를 정의하는가 | 충족 | 1절 표(Product/Work/산출물/Board/게시글/WBS/PBS)와 track·단계·정본 정의. |
| 다 숙지하지 않아도 따라할 수 있는가 | 충족 | 2절 사이클 6단계 + "집기 가능" 목록. 웨이브 에이전트들은 사이클 요약만 받고 참여했다. |
| 스스로 검증되는가 | 충족 | 이 문서와 두 보드가 증거다. 종료·취합 순서·시각·버전 정합이 도구로 기계 판정된다. |
| 적용 범위와 끝이 예상되는가 | 충족 | 보드마다 "Product 한 문장", 끝은 "OPEN·TAKEN 0건 + 루트 DONE". 두 프로젝트 모두 이 조건으로 끝났다. |
| 이 규칙도 그렇도록 노력하는가 | 충족 | 개정 8회 전부가 게시글(Work)로 처리되고 재검증되었다. |

## 4. 남은 과제

다음 개정 후보(불편이 실제로 확인되면 게시글로 올린다):

- 상태 전환(TAKEN/DONE)과 시각 기입을 한 번에 해주는 소도구 — 웨이브 1·2
  에이전트 다수가 손 기입의 번거로움·오기입 여지를 보고했다(검사는 v1.7로
  추가됨).
- 보드 안 시각 정밀도 혼재(분/초)의 표기 통일.
- 저장소가 분리된 에이전트 간 push 선점 규칙의 실전 검증(이번에는 공유 작업
  공간만 실증됨).

해결된 과제: 선후 관계 표기(v1.1) · 시각화(v1.2) · 최종 산출물 위치(v1.2) ·
사이클·검증·다중 산출물(v1.3) · 다중 프로젝트(v1.4) · 트랙/단계(v1.5) ·
에이전트 불편 1차(v1.6) · 조회·정합 검사(v1.7) · 취합 모순(v1.8).
