# PBS WBS Board 규칙

버전: 2.3 (2026-08-21) · 개정 이력: v1.0 `board/000`, v1.1 `board/006`(Gantt 뷰·병렬
진행), v1.2 `board/007`(취합 Work는 마지막·Gantt 종속성 화살표), v1.3
`board/008`(에이전트 사이클·PBS 기록), v1.4 `board/012`(다중 보드), v1.5
`board/013`(track·단계도 Work·병렬 에이전트 운용), v1.6 `board/014`(웨이브 1
에이전트 불편 반영: 초 단위 시각·집기 가능 표시·기록의 정본·잠금 시점), v1.7
`board/015`(읽기 전용 --ready 조회·시각 정합·버전 줄 검사), v1.8
`board/016`(취합 시 자식 산출물 간 모순 처리), v1.9 `board/017`(레벨-에이전트
등급·상태 전환 소도구), v2.0 `board/018`(대용량 보드 뷰), v2.1 `board/019`(대규모 웨이브: 잎 배치
처리·정체 TAKEN 인계·DONE 불가역), v2.2 `board/020`(id 접두어 충돌 정정), v2.3 `board/021`(Work의 단위·발견되는
보드·재작업 루프 — K-SAT 2 오용의 반성)

여러 에이전트와 사람이 함께 하나의 Product를 만들 때, 일을 나누고 주고받는 방식이
서로 달라 생기는 마찰을 없애기 위한 규칙이다.

- **적용 범위**: 원하는 Product가 한 문장으로 정해진 하나의 프로젝트.
  한 저장소에 프로젝트(보드)가 여러 개 있어도 된다 — 보드마다 이 규칙이
  독립적으로 적용된다.
- **끝**: Board에 OPEN·TAKEN 게시글이 없고 루트 게시글이 DONE이면 프로젝트가 끝난 것이다.
  그 시점의 PBS가 Product다.

## 1. 용어

| 용어 | 정의 |
|---|---|
| **Product** | 최종적으로 얻고자 하는 것. 시작할 때 한 문장으로 정하고 루트 게시글로 올린다. |
| **Work** | Product 또는 다른 Work의 필요를 해결하는 작업 단위. 수행과 **검증**까지 마치면 **반드시 산출물을 남긴다**. |
| **산출물 (Deliverable)** | Work가 끝날 때 남기거나 갱신하는 파일(문서·코드·데이터 등). Product와 1:1이 아니다 — Product의 일부분일 수 있고, 한 Work가 여러 개를 남길 수도, 이미 있는 파일의 일부만 갱신할 수도 있다. 산출물 없는 Work는 없다. |
| **Board** | 필요한 것을 올리고 가져가는 게시판. **프로젝트마다 보드 디렉토리 하나**를 가지며, 게시글 1건 = 파일 1개다. 이 저장소의 `board/`는 "규칙" 프로젝트의 보드이고, 예시는 `examples/*/board/`처럼 둔다. |
| **게시글 (Post)** | Board에 올라온 하나의 필요, 즉 하나의 Work 요청. `OPEN → TAKEN → DONE`으로 상태가 바뀐다. |
| **WBS** | 게시글들의 `parent` 관계로 만들어지는 Work 분해 트리. |
| **PBS** | DONE 게시글들의 산출물 경로를 모은 것 — 그 시점 Product의 구성. 트리로 보면 분해 트리, 중복을 제거한 경로 목록으로 보면 Product를 이루는 파일들이다. 같은 파일을 여러 Work가 갱신해도 한 번만 나타나므로, 일부만 갱신한 Work는 PBS 구성을 바꾸지 않을 수 있다. |

PBS의 큰 골격이나 WBS의 일부가 처음부터 정해져 있어도 된다. 정해진 부분은 시작할
때 게시글들로 미리 올려 두면 된다. 단, **미리 올리는 것은 "확실히 남에게 요청하게
될 필요"만이다** — 보드는 계획표가 아니라 필요의 기록이므로, 아직 발견되지 않은
필요를 개수 채우듯 나열하지 않는다. 정해지지 않은 부분은 진행하면서 사이클
2·4단계의 발견으로 자연히 만들어진다.

## 2. 바로 따라하기 — 사이클

전체 규칙을 숙지하지 않아도 아래 사이클만 돌면 된다. 에이전트와 사람 모두
동일하며, **여러 명이 이 사이클을 각자 병렬로 돈다.** 누군가 무엇을 하고
있어도 상관없다 — 각자 자기 사이클을 돌면 Board 위에서 저절로 맞물린다.

1. **집기** — 보드에서 `status: OPEN`이고 `after`의 게시글이 모두 DONE인
   게시글을 하나 골라 `TAKEN`으로 바꾸고, `owner`에 자기 이름, `started`에 현재
   시각을 쓴다. 어떤 게시글을 집을 수 있는지는 뷰 도구가 알려준다(5절 —
   "집기 가능" 표시).
2. **필요 올리기** — 이 Work에 필요한 것(다른 산출물이거나 산출물의 일부분)을
   새 게시글(`OPEN`, `parent`=지금 게시글 id)로 올린다. 순서가 필요한 것만
   `after`로 묶고, 나머지는 비워 두어 병렬로 진행되게 한다. 필요한 것이 없으면
   생략한다.
3. **모이면 수행·검증** — 필요(자식 게시글)가 모두 DONE되면, 그 산출물들을
   입력으로 Work를 수행하고 **검증**한다. 필요가 모이길 기다리는 동안에는
   다른 OPEN 게시글을 집는다(1로) — 그 필요를 내가 집어서 해결해도 된다.
4. **부족하면 다시 올리기** — 수행·검증 중에 새로 필요한 것이 생기면 2처럼
   다시 게시글로 올린다. 몇 번이고 반복해도 된다.
5. **산출물 기록 (PBS)** — 수행과 검증이 끝나면 산출물 경로(들)를
   `deliverable`에 쉼표로 구분해 적고, 검증 방법·결과를 본문에 한 줄 남기고,
   `finished`에 현재 시각을 쓰고 `DONE`으로 바꾼다. 이로써 산출물이 PBS에
   기록된다 — 이미 PBS에 있는 파일을 일부만 갱신했다면 PBS 구성은 그대로다.
   `python3 tools/build_board_view.py`로 `board.html`을 갱신해 함께 커밋한다.
6. **반복** — 1로 돌아가 다른 OPEN 게시글을 집는다. 부모의 DONE은 자식이 모두
   DONE된 다음이므로, 부모의 `finished`는 언제나 자식들보다 늦고 루트
   게시글(최종 Work)이 시간상 맨 마지막에 끝난다.

OPEN·TAKEN 게시글이 없고 루트 게시글이 DONE이면 끝난 것이다.

## 3. 게시글 양식

파일명은 `<보드 디렉토리>/<id>-제목.md`. `id`는 그 보드 안에서 유일하면 된다 —
기본은 만든 순서대로 붙이는 3자리 일련번호(`012`)이고, 프로젝트 접두어를 붙인
형태(`S001`)나 계층 접두어(`QM-STR-BR-01`)도 된다. `parent`·`after`는 같은
보드의 id만 가리킨다. 계층 접두어를 쓰면 한 id가 다른 id의 접두어가 될 수
있으므로, id로 게시글을 찾을 때는 파일명 글롭이 아니라 frontmatter `id`의
**정확 일치**로 판정한다(도구가 이를 보장한다).

```markdown
---
id: 003
title: HTML 뷰 생성 도구 만들기
status: OPEN
parent: 000
owner: -
deliverable: -
after: 001
track: -
started: -
finished: -
---

무엇이 왜 필요한지 2~3문장으로 쓴다.
산출물이 무엇이어야 하는지 한 문장으로 쓴다.
```

- `status`: `OPEN`(누구나 집을 수 있음) → `TAKEN`(owner가 작업 중) → `DONE`(산출물 있음).
- `parent`: 이 필요를 만든 게시글의 id. 루트 게시글은 `-`.
- `owner`: TAKEN으로 바꿀 때 기록. `deliverable`: DONE으로 바꿀 때 산출물
  경로(들)를 쉼표로 구분해 기록. 여러 개여도, 이미 있는 파일의 갱신이어도 된다.
- `after`: 이 Work를 시작하기 전에 DONE되어 있어야 하는 게시글 id들(쉼표 구분).
  없으면 `-`. **`after`로 묶이지 않은 게시글끼리는 언제나 병렬로 진행할 수 있다.**
  `parent`는 취합 구조일 뿐 순서가 아니다.
- `track`: 이 Work가 속한 모듈/분야 이름(선택, 없으면 `-`). 같은 track의
  Work들을 뷰에서 배지로 묶어 보기 위한 것이다. 순서·구조에는 아무 영향이
  없다 — 순서는 `after`, 구조는 `parent`가 전부다.
- `started`: TAKEN으로 바꿀 때, `finished`: DONE으로 바꿀 때 현재 시각을
  `YYYY-MM-DD HH:MM[:SS]`로 기록(짧은 Work가 많으면 초까지 쓰는 편이 Gantt에
  좋다). 이 두 값으로 Gantt 뷰가 그려진다.
  부모의 `finished`는 모든 자식의 `finished`보다 늦어야 한다(4절 취합 규칙).
- **기록의 정본**: 산출물 경로의 정본은 frontmatter `deliverable`이다(본문의
  "산출물: ..." 문장은 무엇이어야 하는지에 대한 요구 설명일 뿐이다). 검증
  기록의 정본은 게시글 본문의 "검증: ..." 줄이다(산출물 파일 안의 검증 절은
  선택). 같은 내용을 두 곳에 맞춰 쓰느라 애쓰지 않는다.
- 상태 변경은 파일 수정 + 커밋으로 한다. 커밋 이력이 곧 시간 기록이다.
  손 편집 대신 `python3 tools/post.py take <게시글> <owner>` /
  `post.py done <게시글> <산출물경로> [검증 한 줄]` / `post.py ready <보드>`를
  쓰면 시각이 초 단위로 자동 기록되고, 선행 미완·상태 순서 위반이 거부된다.
- 두 에이전트가 같은 게시글을 집으면(push 충돌) 먼저 push된 쪽이 owner다.
  밀린 쪽은 되돌리고 다른 OPEN 게시글을 집는다.
- **같은 작업 공간(체크아웃)을 공유하는 병렬 에이전트들**은 push 대신 파일
  소유로 충돌을 피한다: 자기가 집은 게시글 파일과 자기 산출물 파일만
  수정하고, 남의 파일은 읽기만 한다. 이때 게시글 파일을 TAKEN으로 수정하는
  것 자체가 공표(잠금)다 — push 선점 규칙은 저장소를 따로 쓰는 에이전트
  사이에만 적용된다. 커밋과 뷰 갱신은 취합자가 웨이브(병렬 구간) 단위로
  몰아서 해도 된다 — 이때도 게시글의 `started`/`finished`가 시간 기록이므로
  Gantt는 정확하다.

## 4. Work의 규칙

- **Work의 단위는 "남이 해줘야 하는 필요"다.** 게시글은 owner가 바뀌는
  지점(인수인계 경계)에만 생긴다. 혼자(자기 등급으로) 끝낼 수 있는 것은
  쪼개지 말고 한 Work의 **내부 단계**로 삼아, 게시글 본문에 작업
  기록(체크리스트: 한 단계 = 한 줄)으로 남긴다. 쪼개는 정당한 이유는 세
  가지뿐이다: ① 다른 능력·등급·사람이 필요하다 ② 서로 다른 owner가 병렬로
  해야 한다 ③ 다른 Work의 산출물이 입력으로 필요하다(외부 종속). 이유 없이
  잘게 쪼갠 보드는 계획표이지 보드가 아니다.
- Work는 항상 산출물이 나온다고 가정한다. 산출물을 정의할 수 없으면 그 Work는
  아직 덜 쪼개진 것이니 더 쪼갠다. (더 쪼갤 때도 위의 단위 기준을 지킨다 —
  내부 단계로 충분하면 게시글을 만들지 않는다.)
- **재작업은 정상 경로다.** 검증에서 불합격하면: 원인이 자기 수행이면
  내부에서 고치고 작업 기록에 남긴 뒤 DONE한다. 원인이 입력(남의 산출물)이면
  그 track에 수정 필요를 게시글로 올리고(parent=자기 게시글), 수정이 DONE된
  뒤 재검증해서 끝낸다 — 불합격→수정 요청→재검증의 왕복이 기록에 보여야
  한다. 같은 산출물 파일이 여러 Work로 거듭 갱신되는 것이 정상이다(PBS에는
  한 번 나타난다).
- **산출물은 입력을 밝힌다.** 산출물 파일 머리에 "입력: <사용한 다른 산출물
  경로들>"을 적는다. 외부 입력 없이 만들어지는 산출물은 루트 요구사항
  하나뿐이어야 정상이다 — 입력 없는 산출물이 많다면 보드가 계획표가 됐다는
  신호다. after가 track 경계를 넘는 것(인터페이스 종속)은 자연스럽다 —
  그것이 게시글을 만든 이유이기 때문이다.
- **수행만으로는 DONE이 아니다.** Work는 검증까지 끝나야 DONE이며, 검증
  방법과 결과를 게시글 본문에 남긴다. 검증 중에 생긴 필요도 게시글로
  올린다(2절 4단계) — 검증도 Work를 낳는다.
- **산출물은 Product와 1:1이 아니다.** 한 Work가 여러 파일을 남길 수도, 이미
  있는 파일의 일부만 갱신할 수도 있다. `deliverable`에는 남기거나 갱신한
  경로를 모두 적는다.
- 자식(필요) 게시글의 산출물만으로 부모 Work를 수행할 수 있어야 한다.
  부족하면 자식 게시글을 더 올린다.
- **단계도 Work다.** 한 모듈이 여러 단계(BB→EM→FM, STM→FM, 시뮬레이션→EM…)를
  거치며 성숙해 간다면, 단계마다 게시글을 올리고 같은 `track`을 적고 바로 앞
  단계를 `after`로 건다. 단계 구성은 모듈마다 다르며 규칙은 어떤 단계
  집합(EM/QM/FM 등)도 강제하지 않는다 — 무엇을 몇 단계로 나눌지는 그 Work를
  하는 쪽이 정한다. 앞 단계의 산출물이 있는 파일을 다음 단계가 갱신해도 되고
  (PBS에는 한 번 나타난다), 단계마다 다른 파일을 남겨도 된다.
- 산출물은 저장소 안의 파일로 남긴다. 경로가 없으면 DONE이 아니다(도구가
  경고한다).
- **정형화된 잎 Work 다수는 배치로 돈다.** 같은 꼴의 잎 Work가 실제 필요로
  수십 건 쌓였다면(예: 같은 요청이 여러 트랙에서 온 경우), 취합자가 순서를
  담은 실행목록(runlist)을 남기고 잎을 집는 에이전트는 루프로 일괄
  처리한다(한 건씩 손으로 하다 정체되는 것이 병목이다). 한 건이라도
  사이클(take → 산출물 → done)은 그대로다. 단, 배치를 위해 내부 단계를
  게시글로 만들어 건수를 불리지 않는다 — Work의 단위 기준이 먼저다.
- **DONE은 되돌리지 않는다.** 이미 DONE인 게시글을 OPEN으로 리셋해 재실행하는
  것은 기록 덮어쓰기다. 잘못 끝난 Work는 정정 게시글을 새로 올려 고친다(취합
  모순 규칙과 동일). 도구의 "OPEN이 아니다" 거부는 이 규칙의 방어선이다.
- **정체된 TAKEN은 인계한다.** TAKEN인 채 진행이 멈춘 게시글(에이전트 이탈
  등)은 취합자가 정체로 판단하면 owner를 교체해 이어받을 수 있다. 교체
  사실(전 owner, 사유)을 게시글 본문에 한 줄 남긴다 — 기록은 덮어쓰지 않고
  쌓는다.
- **레벨이 깊을수록 가벼운 에이전트가 집는다.** 레벨 = 루트에서의 트리
  깊이(루트 L0). 분해가 잘 되었다면 깊은 레벨의 Work일수록 작고 정형화되어
  있으므로, 더 싸고 가벼운 에이전트(사람이라면 더 짧은 검토)로 충분하다.
  얕은 레벨의 Work(취합·판단·형상 결정)는 비싼 에이전트나 사람이 집는다.
  거꾸로, 가벼운 에이전트가 집기에 무거운 잎 Work가 있다면 그것은 덜 쪼개진
  것이다(분해 품질의 신호). `owner` 이름에 등급이 드러나게 짓는다(예:
  `haiku-eps`, `sonnet-ait`, `claude`). 실행 비용은 잎에, 판단 비용은 취합에
  모인다.
- **병렬이 기본이다.** 여러 에이전트가 서로 다른 게시글을 동시에 TAKEN하고 동시에
  진행한다. 순서 제약은 `after`에 적힌 것이 전부이며, 적지 않은 순서는 없는
  것이다. 쪼갤 때는 `after`를 최소한으로만 걸어 병렬로 진행할 수 있는 폭을
  넓힌다.
- **취합 중 자식 산출물끼리 모순되면**: 나중 단계·실측이 앞 단계·예측을
  이긴다(더 최근에 확정된 사실이 정본). 취합 산출물에 어느 값을 채택했고
  왜인지 한 줄 주석을 남긴다. 값 차이가 단순한 시점 차이가 아니라 자식
  산출물 자체의 결함이면, 그 자식의 DONE을 되돌리지 말고 정정을 새
  게시글(같은 track, 결함 게시글을 본문에서 지목)로 올려서 고친다 — 기록은
  덮어쓰지 않고 쌓는다.
- **취합 Work는 마지막에 끝난다.** 부모는 자식이 모두 DONE된 뒤에 취합하고
  DONE이 되므로, 부모의 `finished`는 항상 모든 자식의 `finished`보다 늦다.
  따라서 루트 게시글(최종 Work)의 `finished`는 전체 타임라인에서 항상 마지막이다.
  이미 DONE인 부모 아래에 새 자식이 생기면(개정 등) 부모를 다시 `TAKEN`으로
  되돌리고, 자식이 끝난 뒤 재취합해 `finished`를 현재 시각으로 갱신하고 `DONE`으로
  만든다. 이 순서가 깨진 게시글은 `tools/build_board_view.py`가 경고한다.

## 5. 취합: PBS와 WBS, 그리고 HTML 뷰

- **WBS** = 게시글 트리 그 자체(상태 포함).
- **PBS** = DONE 게시글들의 `deliverable` 경로를 모은 것. 구조로 합치면 분해
  트리가 되고, 중복을 제거해 모으면 그 시점 **Product의 구성**(경로별로
  만들거나 갱신한 Work들과 마지막 갱신 시각)이 되고, 커밋(또는 finished)
  순서로 합치면 시간 순 산출물 목록이 된다.
- Board는 에이전트만 보는 것이 아니다. 사람을 위해
  `python3 tools/build_board_view.py [보드 디렉토리] [출력 html]`로 보드마다
  HTML 뷰를 만들어 둔다. 인자를 생략하면 `board/`와 `board.html`(규칙
  프로젝트)이다. 링크는 출력 파일 위치 기준으로 계산되므로 보드가 어디에
  있어도 된다.
  이 페이지는 WBS 트리, **Gantt 차트**, PBS(Product 구성과 분해 트리), 시간 순
  산출물 목록, 진행 요약을 보여준다. Gantt는 각 Work의 `started`~`finished` 구간을 막대로 그리고
  (병렬 구간은 겹쳐 보인다), **`after` 종속성을 선행 Work의 끝에서 후행 Work의
  시작으로 잇는 화살표**로 그린다. 취합 순서 위반(자식보다 먼저 끝난 부모,
  자식이 안 끝났는데 DONE인 부모, 마지막이 아닌 루트)은 경고로 표시한다.
  게시글을 바꾼 커밋에는 가능하면 갱신된 뷰를 함께 담되, 병렬 구간에서는
  취합자가 웨이브 단위로 한 번만 갱신해도 된다(3절 병렬 운용). WBS·Gantt의
  각 게시글에는 `track` 배지가 붙어 같은 모듈의 단계들이 한눈에 보인다.
  OPEN이면서 `after`가 모두 DONE인 게시글은 **"집기 가능"**으로 표시되고,
  도구가 콘솔에도 그 목록을 출력한다 — 사이클 1단계는 이 목록에서 고르면
  된다. 병렬 웨이브 중간처럼 뷰 파일을 건드리면 안 될 때는
  `python3 tools/build_board_view.py <보드> --ready`로 **아무 파일도 쓰지 않고**
  집기 가능 목록만 조회한다.
- 도구는 취합 순서 위반 외에도 `started`가 `finished`보다 늦은 게시글(시각
  오기입)과, RULES.md 머리의 버전 줄이 개정 이력의 마지막 항목과 다른 것을
  경고한다.
- **큰 보드(게시글 100건 초과)에서는 뷰가 요약 중심으로 바뀐다**: 레벨×상태
  요약표와 트랙 요약표(진행률 막대), 접히는 WBS/PBS 트리(부모마다 하위
  진행률 n/m, 레벨 2까지 펼침), 요약 Gantt(레벨 2까지 — 하위는 부모 막대에
  합쳐 보인다) + 서브트리별 상세 Gantt(접힘), 디렉토리로 묶인 Product 구성,
  트랙별로 묶인 시간 순 목록. 화살표는 같은 Gantt 안의 선행만 그리고, 밖의
  선행은 라벨의 "선행: "으로 남는다.

## 6. 자기 검증

이 규칙은 이 규칙으로 만들어졌다. `board/000`~`board/005` 게시글이 v1.0의
기록이고(개정은 7절에 따라 `board/006`부터 이어진다),
산출물이 이 저장소의 파일들(RULES.md, tools/build_board_view.py, board.html,
VALIDATION.md)이다. 루트 게시글 000이 DONE이므로 규칙 v1.0은 스스로 검증되어
완료다. 검증 체크리스트와 결과는 `VALIDATION.md`에 있다.

## 7. 규칙의 발전

규칙은 계속 검토하며 발전해야 한다.

- 규칙을 고치고 싶은 것도 하나의 **필요**다. `board/`에 게시글(루트의 자식)로
  올리고, 산출물로 새 버전의 `RULES.md`(버전 번호 올림)와 갱신된 `VALIDATION.md`를
  남긴다. 이때 루트 게시글은 다시 `TAKEN`으로 되돌리고, 개정 게시글이 DONE된 뒤
  재취합해 루트의 `finished`를 갱신한다(4절) — 최종 Work가 항상 마지막에 오도록.
- 검토 주기: 루트 게시글이 DONE될 때(프로젝트가 끝날 때)마다, 이번에 규칙 때문에
  불편했던 점을 게시글로 올린다. 불편이 없으면 올리지 않아도 된다.
- 새 버전도 6절과 같은 방식으로 스스로 검증되어야 완료다.
